# RotaV

Para rodar a aplicação é necessário criar o banco de dados, 
execute o script 'create_insert_Rota_Viagem.sql' no SQL Server,
que está dentro da pasta 'Banco de Dados'.

Altere a ConnectionStrings no 'appsettings.json'

Evidências de testes, execuções e prints na pasta 'Docs'
